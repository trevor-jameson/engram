{
  "name": "@engram/server",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "dev": "ENGRAM_VAULT_PATH=../scratch-vault node --watch index.ts"
  },
  "dependencies": {
    "@engram/shared": "workspace:*",
    "gray-matter": "^4.0.3"
  }
}
