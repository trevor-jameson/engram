// Placeholder proving the Vite dev server runs. The real UI (React + MUI)
// arrives in unit 05.
const root = document.getElementById("root");
if (root !== null) {
  root.textContent = "Engram — scaffold placeholder (unit 01)";
}
